<h3><%= link_to 'Projekty', projects_path %> > <%= Project.find(params[:project_id]).workname %> > <%= link_to 'Akce', project_events_path(@project) %> > Nová akce</h3>

<%= render 'form' %>

<%= link_to 'Back', project_events_path %>
