

<%= form_for [@project,@eventgroup] do |f| %>
  <% if @eventgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eventgroup.errors.count, "error") %> prohibited this eventgroup from being saved:</h2>

      <ul>
      <% @eventgroup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Název skupiny" %><br />
    <%= f.text_field :name %>
  </div>

  <%= f.hidden_field :project_id, :value => params[:project_id] %>

  <div class="field">
    <%= f.label :user_ids, "Účastníci" %><br />
    <%= f.collection_select :user_ids, User.order(:surname), :id, :sur_firstname ,{},{multiple: true}   %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
